# คู่มือการนำขึ้นใช้งานบน Vercel (Deployment Guide)
### ระบบคำนวณการคืนพัสดุประเภทเศษเหล็ก การไฟฟ้าส่วนภูมิภาค (PEA)

โปรเจกต์นี้ได้รับการจัดโครงสร้างให้พร้อม Deploy ขึ้นใช้งานบน **Vercel** ได้ทันทีโดย **ไม่ต้องใช้ Database (ฐานข้อมูล)** และไม่มีค่าใช้จ่าย (ฟรีบน Hobby Plan)

---

## สรุปโครงสร้างไฟล์ที่เตรียมไว้สำหรับ Vercel

- 📁 **`api/index.py`**: Vercel Serverless Function รองรับการอ่านและคำนวณไฟล์ PDF/Excel จาก SAP
- 📄 **`requirements.txt`**: ระบุไลบรารีที่ Vercel Python Runtime ต้องติดตั้ง (`pdfminer.six`, `openpyxl`)
- ⚙️ **`vercel.json`**: คอนฟิกการเชื่อมโยงเส้นทาง (Routing & Rewrites)
- 📁 **`samples/`**: รวบรวมไฟล์ตัวอย่างรายงาน 018 (`018.pdf`, `018-กำแพงแสน.pdf`, `018-PED0001201261_1.pdf`) เพื่อให้สามารถกดทดสอบคำนวณบน Vercel ได้ทันทีโดยไม่ต้องอัปโหลดไฟล์
- 📄 **`master_data.json`**: ฐานข้อมูลน้ำหนักอุปกรณ์ 71 รายการ

---

## ขั้นตอนการนำขึ้น Vercel (วิธีที่ง่ายที่สุด: ผ่าน GitHub)

### ขั้นตอนที่ 1: สร้าง Repository บน GitHub
1. เปิดเว็บไซต์ [GitHub.com](https://github.com) และเข้าสู่ระบบ
2. กดปุ่ม **New** (หรือเครื่องหมาย **+** มุมขวาบน) เพื่อสร้าง Repository ใหม่
3. ตั้งชื่อ Repository เช่น: `pea-scrap-calculator`
4. เลือกระดับความปลอดภัยเป็น **Public** หรือ **Private** ก็ได้ตามต้องการ
5. **ไม่ต้องติ๊ก** Add a README file (เพราะในโฟลเดอร์ของเรามีไฟล์ครบแล้ว)
6. กด **Create repository**

---

### ขั้นตอนที่ 2: นำโค้ดขึ้น GitHub จากเครื่องคอมพิวเตอร์
เปิดหน้าต่าง Terminal หรือ PowerShell ในโฟลเดอร์ `d:\Calculate_` แล้วรันคำสั่งดังนี้:

```bash
git add .
git commit -m "Deploy PEA Scrap Metal Calculator to Vercel"
git branch -M main
git remote add origin https://github.com/<ชื่อผู้ใช้GitHubของคุณ>/<ชื่อRepositoryของคุณ>.git
git push -u origin main
```
*(เปลี่ยน URL เป็นลิงก์ Repository ที่ได้จากขั้นตอนที่ 1)*

---

### ขั้นตอนที่ 3: เชื่อมต่อและ Deploy บน Vercel
1. เปิดเว็บไซต์ [Vercel.com](https://vercel.com) และเข้าสู่ระบบ (แนะนำให้ล็อกอินด้วยบัญชี **GitHub**)
2. ในหน้า Dashboard กดปุ่ม **"Add New..."** แล้วเลือก **"Project"**
3. ใต้หัวข้อ **Import Git Repository** จะพบชื่อโปรเจกต์ `pea-scrap-calculator` ที่เพิ่งอัปโหลด
4. กดปุ่ม **"Import"** ข้างชื่อโปรเจกต์
5. ในหน้าตั้งค่าโปรเจกต์ (Configure Project):
   - **Framework Preset**: ปล่อยเป็น `Other`
   - **Root Directory**: ปล่อยว่างไว้ (`./`)
   - **Build and Output Settings**: ปล่อยตามค่าเริ่มต้น (Vercel จะอ่านจาก `vercel.json` อัตโนมัติ)
   - **Environment Variables**: ไม่จำเป็นต้องใส่
6. กดปุ่ม **"Deploy"**

---

### ขั้นตอนที่ 4: เข้าใช้งานเว็บไซต์
- รอ Vercel ดำเนินการติดตั้งไลบรารีและสร้างระบบประมาณ 30 - 60 วินาที
- เมื่อเสร็จสิ้น จะมีหน้าจอแสดงความยินดีพร้อม URL ประจำเว็บไซต์ เช่น:
  `https://pea-scrap-calculator.vercel.app`
- สามารถคัดลอกลิงก์นี้ส่งให้เพื่อนร่วมงานหรือเปิดใช้งานได้ทันทีจากทุกอุปกรณ์ (คอมพิวเตอร์, แท็บเล็ต, สมาร์ตโฟน) ตลอด 24 ชั่วโมง

---

## ฟังก์ชันการใช้งานบน Vercel

1. **ทดสอบด้วยไฟล์ตัวอย่าง (Sample Files)**:
   - ผู้ใช้สามารถกดเลือกไฟล์ตัวอย่างจาก Dropdown ด้านบน เช่น `018-กำแพงแสน.pdf (งานรื้อถอน)` เพื่อทดสอบการคำนวณได้ทันที
2. **อัปโหลดไฟล์งานจริง (Upload Your File)**:
   - ลากไฟล์รายงาน ZPSR018 (PDF หรือ Excel) จาก SAP มาวางในกล่องอัปโหลด ระบบจะแยกตารางและคำนวณน้ำหนักเศษเหล็กให้อัตโนมัติ
3. **พิมพ์รายงาน กฟภ.**:
   - กดแท็บ "แบบฟอร์มพิมพ์รายงาน กฟภ." เพื่อดูตัวอย่างและสั่งพิมพ์หรือบันทึกเป็น PDF ได้ทันที

---

## การอัปเดตเวอร์ชันใหม่ในอนาคต

เมื่อมีการแก้ไขหรือปรับปรุงโค้ดในเครื่องคอมพิวเตอร์ เพียงรันคำสั่ง:
```bash
git add .
git commit -m "Update feature"
git push
```
ระบบ Vercel จะตรวจจับการเปลี่ยนแปลงและทำการ **Auto-Deploy เวอร์ชันใหม่ให้อัตโนมัติทันที**
